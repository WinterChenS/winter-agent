# V2.9 Expert Team — Reviewer / Judge

> 基于路线图推演。

## 1. 概述

- **目标**：实现自动 Review、评分和纠错机制
- **状态**：📋 规划中

## 2. 技术方案

```python
class Reviewer:
    async def review(self, output: str, context: ReviewContext) -> ReviewResult:
        """代码/文档质量审查"""
        checks = [
            self._check_correctness(output, context),
            self._check_completeness(output, context),
            self._check_best_practices(output, context),
            self._check_security(output, context),
        ]
        return ReviewResult(score=avg(s.score), findings=checks)

class Judge:
    async def evaluate(self, submissions: list[TeamOutput]) -> JudgeResult:
        """多方案评估和排名"""
```

## 3. Phase 拆分

| Phase | 名称 | 预估 |
|-------|------|------|
| 1 | Review Engine | 3d |
| 2 | Code Review Rules | 2d |
| 3 | Scoring System | 2d |
| 4 | Auto-fix Suggestions | 2d |
| 5 | Judge Evaluation | 2d |

## 4. 依赖：V2.8（Expert Roles）
