# AI Chat V0.1 测试文档

## 功能测试清单

### 1. 前端 UI 测试

测试步骤：
1. 访问 http://localhost:3000
2. 检查页面布局是否正常
3. 确认显示欢迎界面和机器人图标

预期结果：
- ✅ 页面布局正常，无 UI 错位
- ✅ Header 显示标题 "AI Chat V0.1"
- ✅ 显示欢迎消息和机器人图标
- ✅ 输入框和发送按钮正常显示

### 2. 发送消息测试

测试步骤：
1. 在输入框输入 "你好"
2. 点击发送按钮
3. 再次输入 "今天天气如何" 并按 Enter 键

预期结果：
- ✅ 用户消息显示在右侧（蓝色背景）
- ✅ AI 消息显示在左侧（灰色背景）
- ✅ 发送按钮在发送过程中置灰
- ✅ 发送后输入框清空
- ✅ Enter 键可以发送消息

### 3. 流式响应测试

测试步骤：
1. 发送 "请写一篇关于春天的短文"
2. 观察 AI 回复的显示效果

预期结果：
- ✅ 1-3 秒内看到第一个字
- ✅ 文字逐字显示（打字机效果）
- ✅ 流式输出过程中无明显卡顿
- ✅ 输出完成后可以再次发送消息

### 4. Markdown 渲染测试

测试步骤：
发送以下消息：
```
请用 Markdown 格式展示：
1. 加粗文字
2. 代码块
3. 列表
```

预期结果：
- ✅ 加粗文字正确显示
- ✅ 代码块有语法高亮
- ✅ 列表正确渲染
- ✅ 换行正确显示

### 5. 错误处理测试

测试步骤：
1. 停止 AI 服务
2. 发送消息 "测试错误"

预期结果：
- ✅ 显示错误提示 "AI 服务繁忙，请稍后再试"
- ✅ 输入框重新启用
- ✅ 可以继续发送消息

### 6. 自动滚动测试

测试步骤：
1. 连续发送多条消息
2. 观察聊天窗口滚动行为

预期结果：
- ✅ 新消息出现时自动滚动到底部
- ✅ 流式输出过程中保持底部可视

## 性能测试

### 压力测试脚本

```bash
# 使用 k6 进行压力测试
k6 run load-test.js
```

### 性能指标目标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 首字时间 (TTFT) | <3 秒 | 发送消息到第一字出现 |
| 流式延迟 | <100ms/token | 相邻 token 间隔 |
| 并发用户 | 100+ | 同时对话用户数 |
| 错误率 | <1% | 失败请求比例 |

## API 测试

### 使用 curl 测试后端接口

```bash
curl -X POST http://localhost:8080/api/chat/stream \
  -H "Content-Type: application/json" \
  -d '{"message": "你好"}'
```

### 使用 curl 测试 AI 服务

```bash
curl -X POST http://localhost:8000/api/v1/generate/stream \
  -H "Content-Type: application/json" \
  -d '{"message": "你好", "stream": true}'
```

## 验收检查清单

### 功能验收

- [ ] 前端页面布局正常，无明显 UI 错位
- [ ] 用户可以正常发送消息（按钮 + 回车）
- [ ] 消息发送后 1-3 秒内看到第一个字
- [ ] AI 回复呈现明显的打字机流式效果
- [ ] 对话内容准确，无乱码
- [ ] Markdown 格式正确渲染（代码块、加粗、换行）
- [ ] 消息窗口自动滚动到底部
- [ ] AI 服务不可用时显示统一错误提示

### 技术验收

- [ ] 代码结构清晰，符合各技术栈最佳实践
- [ ] 前端使用 TypeScript 类型安全
- [ ] 后端使用 WebFlux 响应式编程
- [ ] AI 服务使用 LangGraph 状态图架构
- [ ] 配置文件完整
- [ ] Docker 配置可正常运行

### 文档验收

- [ ] README.md 完整清晰
- [ ] API 接口文档完整
- [ ] 部署说明明确
- [ ] 环境变量说明完整

## 已知问题

暂无

## 测试报告模板

```
测试日期：YYYY-MM-DD
测试人员：XXX
测试环境：本地开发环境

测试结果：
- 功能测试：通过 X 项，失败 Y 项
- 性能测试：TTFT 平均 X 秒
- 问题记录：
  1. [问题描述]
  2. [问题解决情况]

总体评价：[通过/不通过]
```
