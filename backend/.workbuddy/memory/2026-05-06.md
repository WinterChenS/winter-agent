# 2026-05-06 工作日志

## 项目迁移：Kotlin Gradle → Java 21 Maven

- 将后端项目从 Kotlin + Gradle 完整迁移为 Java 21 + Spring Boot 3.4.5 + Maven
- 构建工具：删除 build.gradle.kts / settings.gradle.kts / gradlew，新建 pom.xml
- 所有 Kotlin data class 替换为 Java 21 record（ChatRequest, ChatResponse, GenerateRequest, GenerateResponse）
- Spring Boot 主类、WebFluxConfig、AIClient、ChatService、ChatController 均改写为纯 Java
- Dockerfile 更新为 maven:3.9.6-eclipse-temurin-21 + eclipse-temurin:21-jre-alpine
- mvn compile 验证通过，零错误
